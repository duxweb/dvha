# 核心组件

Core 组件导出自 `@duxweb/dvha-core`：

```ts
import {
  DuxCan,
  DuxError,
  DuxException,
  DuxLoaderIframe,
  DuxLoaderRemote,
  DuxLogo,
  DuxLogoIcon,
  DuxNotAuthorized,
  DuxNotFound,
  DuxOverlay,
  DuxRemoteRender,
} from '@duxweb/dvha-core'
```
