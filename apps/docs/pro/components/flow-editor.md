# 流程编辑器

导出自 `packages/pro/src/components/flowEditor`。

```ts
import {
  DuxFlowEditor,
  FlowFieldConfig,
  FlowKVInput,
  FlowNodeCard,
  FlowNote,
  FlowSetting,
  FlowToolbar,
  createDynamicFlowNode,
  createDynamicFlowNodes,
  defaultNodes,
  useNodeDataFlow,
} from '@duxweb/dvha-pro'
```
