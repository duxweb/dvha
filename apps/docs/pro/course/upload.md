# 文件上传与管理

上传相关组件位于 `packages/pro/src/components/upload`，常用：

```ts
import { DuxFileUpload, DuxImageUpload, DuxVideoUpload, DuxFileManage } from '@duxweb/dvha-pro'
```

管理端上传配置扩展在 `packages/pro/src/main.ts` 中声明（`IManage.upload`）。
